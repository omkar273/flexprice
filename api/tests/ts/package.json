{
  "name": "flexprice-sdk-tests",
  "private": true,
  "description": "SDK integration tests for @flexprice/sdk (published or local api/typescript)",
  "scripts": {
    "test": "npx ts-node test_sdk.ts",
    "test:published": "npm install --no-save @flexprice/sdk@latest && npx ts-node test_sdk.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@flexprice/sdk": "file:../../typescript"
  }
}
